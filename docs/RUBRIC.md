# Rubric self-grade (assessment 9)

Write answers in your notes **before** opening the key in the app (`rubric <id>`).
Each key bullet = 1 point. Pass = `passScore` bullets.

In-app: `rubric` lists prompts · `rubric r-expose-publish` shows one key.

Capstone: `r-capstone-explain` (pass ≥ 6) plus the in-app `reg-capstone-gate` + real lab 12.

## Suggested assessment weight

| Piece | Weight |
|-------|--------|
| Level wins (par respected) | 30% |
| Quiz bank | 15% |
| Rubric essays (12) | 25% |
| Real Docker scorecard (`labs/SCORECARD.md`) | 20% |
| Capstone gate + explanation | 10% |

## Authenticity (dimension 4)

Do **not** claim authenticity 9 from the web app alone. Required: `labs/run-all.ps1` (or `.sh`) on a live daemon with score **≥ 20/24** on the scorecard.
